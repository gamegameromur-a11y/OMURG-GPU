name: Build Android
on:
  push:
    branches:
      - main  # Eğer ana dalın adı 'master' ise burayı master yap
  workflow_dispatch: # Manuel başlatma butonunu açar

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout projesi
        uses: actions/checkout@v3

      - name: 🟢 Node.js kurulumu
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: 📦 Kütüphaneleri yükle (npm install)
        run: npm install

      - name: 🛠 EAS CLI kurulumu
        run: npm install -g eas-cli

      - name: 🚀 Expo Build Başlat
        run: |
          # Önce projeyi bağla (ID hatasını çözer)
          eas init --id ${{ secrets.EXPO_PROJECT_ID }} --non-interactive
          # Sonra derlemeyi başlat
          eas build --platform android --profile production --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
